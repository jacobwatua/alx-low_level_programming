#include <string.h>
#include "main.h"
/**
 * _strlen - name of the function
 * @s: charcter to be checked
 *
 * gets the length of a string
 * Return: The lenght of str s
*/
int _strlen(char *s)
{
int str_len = strlen(s);
return (str_len);
}

