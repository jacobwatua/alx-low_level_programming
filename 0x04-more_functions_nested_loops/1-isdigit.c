#include "main.h"
/**
 * _isdigit - Name of the func
 * @c: one integer argument passed
 *
 * Description: checkcs if a argument is an integer
 * Return: 1 if true 0 if not true
*/
int _isdigit(int c)
{
if (c >= '0' && c <= '9')
{
return (1);
}
else
{
return (0);
}
}

